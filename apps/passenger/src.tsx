import React,{useState}from"react";import{createRoot}from"react-dom/client";import{apiClient,money}from"@libswiftride/sdk";import{Action,Map,Shell,Stat}from"@libswiftride/ui";import"@libswiftride/ui/styles.css";
type Quote={fareMinor:number;driverEarningsMinor:number;companyCommissionMinor:number;estimatedDurationSec:number};
function App(){const[quote,setQuote]=useState<Quote|null>(null);const[message,setMessage]=useState("");const[promo,setPromo]=useState("");
const locations={pickup:{address:"Broad Street, Monrovia",latitude:6.3156,longitude:-10.8074},destination:{address:"Samuel K. Doe Sports Complex",latitude:6.3058,longitude:-10.7492}};
async function getQuote(){try{const r=await apiClient.request<{data:Quote}>("/rides/quote",{method:"POST",body:JSON.stringify({...locations,promoCode:promo||undefined})});setQuote(r.data);setMessage("")}catch(e){setMessage((e as Error).message)}}
async function book(){try{const r=await apiClient.request<{data:{id:string}}>("/rides",{method:"POST",idempotencyKey:crypto.randomUUID(),body:JSON.stringify({...locations,paymentMethod:"CASH",promoCode:promo||undefined})});setMessage(`Ride ${r.data.id.slice(0,8)} requested. Searching for a nearby driver.`)}catch(e){setMessage((e as Error).message)}}
return <Shell product="Passenger"><div className="toolbar"><div><span className="eyebrow">Passenger home</span><h1>Where to?</h1></div><a href="#history">Ride history</a></div>{message&&<p className={message.startsWith("Ride")?"notice":"notice error"}>{message}</p>}<section className="hero"><div className="panel form"><label>Pickup<input value={locations.pickup.address} readOnly/></label><label>Destination<input value={locations.destination.address} readOnly/></label><label>Promo code<input value={promo} onChange={e=>setPromo(e.target.value)} placeholder="Optional"/></label><label>Payment<select><option>Cash</option><option>Orange Money</option><option>MTN MoMo</option><option>Stripe</option></select></label><div className="toolbar"><button className="action" onClick={getQuote}>Get estimate</button>{quote&&<button className="action" onClick={book}>Book now</button>}</div>{quote&&<div className="grid"><Stat label="Fare" value={money(quote.fareMinor)}/><Stat label="Driver receives" value={money(quote.driverEarningsMinor)}/><Stat label="ETA" value={`${Math.round(quote.estimatedDurationSec/60)} min`}/></div>}</div><Map/></section><section id="history"><h2>Your recent rides</h2><p>Authenticated ride history, receipts and ratings load from the ride history API.</p></section></Shell>}createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);
