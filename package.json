{
  "name": "libswiftride",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@10.13.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "test:browser:staging": "playwright test",
    "test:browser:install": "playwright install chromium",
    "format": "prettier --write .",
    "db:generate": "pnpm --filter @libswiftride/api prisma:generate",
    "db:migrate": "pnpm --filter @libswiftride/api prisma:migrate",
    "db:deploy": "pnpm --filter @libswiftride/api prisma:deploy",
    "recovery:validate": "pnpm --filter @libswiftride/api recovery:validate",
    "compose:up": "docker compose up --build"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "prettier": "^3.6.2",
    "turbo": "^2.10.12",
    "typescript": "^7.0.2"
  }
}
